abstract final class TimingConstants {
  static const Duration wakeBuffer = Duration(minutes: 30);
  static const Duration sleepBuffer = Duration(minutes: 30);
  static const int minimumIntervalMinutes = 15;
}
