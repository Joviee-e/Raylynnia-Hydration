name: raylynnia_hydration
description: Raylynnia Hydration - Offline-first hydration reminder app.
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  get_it: ^8.0.3
  flutter_local_notifications: ^17.2.4
  shared_preferences: ^2.5.3
  hive: ^2.2.3
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
